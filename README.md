
# Chrome Tab Mouse Enhancer (AutoHotkey v2)

<a name="english"></a>

## 🇺🇸 English

A lightweight AutoHotkey v2 script that improves tab-bar interactions in Chrome. It also support Edge.

### Features

* **🖱️ Double-Click to Close Tab**
  Double-click the tab bar to close the current tab. *(Default: On)*

* **🔄 Scroll to Switch Tabs**
  When the cursor is over the tab bar, use the mouse wheel to switch tabs. *(Default: On)*

* **❌ Right-Click to Close Tab**
  Optional: close a tab via right-click on the tab bar. *(Default: Off)*

### How to use

1. Go to Release to download the file and run it.
2. Right-click the tray icon to toggle features / switch language / enable startup / hide the tray icon.
3. Hotkey: **Ctrl + Alt + H** to toggle tray icon visibility.

---

<a name="chinese"></a>

## 🇨🇳 中文

一个轻量级 AutoHotkey v2 脚本，用于增强 Chrome 的标签栏操作体验。也支持Edge浏览器。

### 功能

* **🖱️ 双击关闭标签页**
  在标签栏双击即可关闭当前标签页。（默认：开启）

* **🔄 滚轮切换标签页**
  鼠标悬停在标签栏时滚动滚轮切换标签。（默认：开启）

* **❌ 右键关闭标签页**
  可选：右键关闭标签页。（默认：关闭）

### 使用方法

1. 去Release下载文件并运行。
2. 右键托盘图标：切换功能 / 切换语言 / 设置开机启动 / 隐藏托盘图标。
3. 快捷键：**Ctrl + Alt + H** 显示/隐藏托盘图标。
